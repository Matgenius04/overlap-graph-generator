import { defineConfig } from "vite"

export default defineConfig({
    base: '/overlap-graph-generator/',
    build: {
        rollupOptions: {
            // https://rollupjs.org/guide/en/#big-list-of-options
        }
    }
});